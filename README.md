# restaurants-client-flux

##Quick start

```
# NPM
npm install           # Install all dependencies
npm start             # Run the app using webpack-dev-server on localhost:3000
npm test              # Run test using Karma
npm run linter        # Lint code using eslint

